{
  "name": "deathbox-live",
  "version": "2.1.0",
  "private": true,
  "description": "Live multiplayer Deathbox game with shared Redis room storage",
  "main": "lock-server.js",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "start": "node lock-server.js",
    "check": "node --check server.js && node --check lock-server.js && node --check public/lock-mode.js && node --check public/interface-polish.js && node --check public/final-fixes.js"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "^8.3.0",
    "express": "^4.21.2",
    "redis": "^4.7.0",
    "socket.io": "^4.8.1"
  }
}
