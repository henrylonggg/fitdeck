# FitDeck Pro

## Run locally

```bash
npm install
npm start
```

Open http://localhost:3000

## Render configuration

- Service type: Web Service
- Runtime: Node
- Build command: `npm install`
- Start command: `npm start`
- Health check path: `/health`

The application currently stores profile and fitness history in browser localStorage.
